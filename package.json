{
  "private": true,
  "packageManager": "pnpm@10",
  "scripts": {
    "build": "pnpm -C engine build && pnpm -C apps/web build",
    "build:engine": "pnpm -C engine build",
    "build:web": "pnpm -C apps/web build",
    "dev:web": "pnpm -C apps/web dev",
    "preview:web": "pnpm -C apps/web preview"
  },
  "engines": { "node": ">=18.18" }
}